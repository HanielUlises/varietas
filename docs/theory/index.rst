======
Theory
======

What the library computes, stated as mathematics rather than as an API. Nothing
here is required to *use* varietas — :doc:`../quickstart` is enough for that —
but the failure modes are algebraic, and they read as noise unless the
construction behind them is in view.

.. toctree::
   :maxdepth: 2

   formulation
   rationalisation
   ideals
   solving
   exactness
   workspace
   singularities
